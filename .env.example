# Meeting Scheduler MCP - Environment Configuration

# Required IMAP settings
IMAP_HOST=imap.example.com      # Your IMAP server hostname
IMAP_USER=your@email.com        # Your IMAP username/email
IMAP_PASSWORD=your_secure_password   # Your IMAP password
IMAP_FROM=your@email.com        # From address for sent emails

# Optional TLS/SSL Configuration (sensible defaults provided)
# IMAP_PORT=993                  # IMAP port (default: 993 for SSL)
# IMAP_USE_SSL=true              # Use SSL/TLS (default: true)
# IMAP_USE_STARTTLS=false        # Use STARTTLS (default: false)
# IMAP_VERIFY_SSL=true           # Verify SSL certificates (default: true)

# Draft folder configuration
IMAP_DRAFT_FOLDER=INBOX.Drafts  # Default folder for saving email drafts

# Security Note: Never commit this file with real credentials!
# Use environment variables in production instead.